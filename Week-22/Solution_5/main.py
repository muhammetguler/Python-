char = input()
upper_ch = chr(ord(char) - (ord('a') - ord('A')))
print(upper_ch)

#The ord() function returns the number representing the unicode code of a specified character.

#You can convert an int to a char, but not vice versa.

# Built-in function: ch.upper()